apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-fed-demo
  namespace: argocd
spec:
  destination:
    name: ''
    namespace: default
    server: 'https://kubernetes.default.svc'
  source:
    repoURL: https://github.com/onehok1/fedferal-observability-demo.git  
    targetRevision: main
    path: apps/grafana-stack  # <- replace with the actual folder path in your repo
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
